import 'package:flutter/material.dart';

class SliderOptions extends StatelessWidget {
  const SliderOptions({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
